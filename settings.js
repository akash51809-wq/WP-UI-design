document.addEventListener('DOMContentLoaded',()=>{const tabs=[...document.querySelectorAll('.settings-tab')],panels=[...document.querySelectorAll('.settings-panel')];tabs.forEach(tab=>tab.addEventListener('click',()=>{tabs.forEach(t=>t.classList.remove('active'));panels.forEach(p=>p.classList.remove('active'));tab.classList.add('active');document.getElementById(tab.dataset.panel)?.classList.add('active')}));document.querySelectorAll('.toggle').forEach(t=>t.addEventListener('click',()=>t.classList.toggle('on')));document.querySelectorAll('.settings-range').forEach(r=>{const out=r.parentElement.querySelector('.range-value');const sync=()=>{if(out)out.textContent=r.value+' ms'};r.addEventListener('input',sync);sync()});document.querySelectorAll('.settings-save').forEach(b=>b.addEventListener('click',()=>{const old=b.innerHTML;b.innerHTML='✓ Saved';setTimeout(()=>b.innerHTML=old,1200)}));const menu=document.querySelector('[data-target="settings-section"]');if(menu)menu.addEventListener('click',()=>setTimeout(()=>{document.querySelector('.settings-tab.active')?.scrollIntoView({block:'nearest'});},0));});